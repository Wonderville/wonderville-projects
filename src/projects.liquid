---
layout: Page.liquid
title: projects
class: Projects
---
<!-- ~*~ Playtest Night ~*~ -->
<section class="ProjectsSection">
  <h1 class="ProjectsSection-title">
    Playtest Night
  </h1>

  {%- for project in collections.playtests %}
    {% render "projects/Project.liquid", project: project %}
  {%- endfor %}
</section>

<!-- ~*~ Game Jams ~*~ -->
<section class="ProjectsSection">
  <h1 class="ProjectsSection-title">
    Game Jams
  </h1>

  {%- for project in collections.jams %}
    {% render "projects/Project.liquid", project: project %}
  {%- endfor %}
</section>

<!-- ~*~ Residencies ~*~ -->
<section class="ProjectsSection">
  <h1 class="ProjectsSection-title">
    Residencies
  </h1>

  {%- for project in collections.residencies %}
    {% render "projects/Project.liquid", project: project %}
  {%- endfor %}
</section>

<!-- ~*~ Internship ~*~ -->
<section class="ProjectsSection">
  <h1 class="ProjectsSection-title">
    Internship
  </h1>

  {%- for project in collections.internships %}
    {% render "projects/Project.liquid", project: project %}
  {%- endfor %}
</section>