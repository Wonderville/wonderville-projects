---
layout: Page.liquid
title: Projects
---

<!-- ~*~ Internship ~*~ -->
<section class="PageSection">
  <h1 class="ProjectSection-title">
    Internship
  </h1>

  {%- for project in collections.internships %}
    {% render "projects/Project.liquid", project: project %}
  {%- endfor %}
</section>

<!-- ~*~ Residencies ~*~ -->
<section class="PageSection">
  <h1 class="ProjectSection-title">
    Residencies
  </h1>

  {%- for project in collections.residencies %}
    {% render "projects/Project.liquid", project: project %}
  {%- endfor %}
</section>

<!-- ~*~ Game Jams ~*~ -->
<section class="PageSection">
  <h1 class="ProjectSection-title">
    Game Jams
  </h1>

  {%- for project in collections.jams %}
    {% render "projects/Project.liquid", project: project %}
  {%- endfor %}
</section>

<!-- ~*~ Playtest Night ~*~ -->
<section class="PageSection">
  <h1 class="ProjectSection-title">
    Playtest Night
  </h1>

  {%- for project in collections.playtests %}
    {% render "projects/Project.liquid", project: project %}
  {%- endfor %}
</section>